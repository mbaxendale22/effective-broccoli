<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/fourways_logo.svg" type="image/svg+xml">
    <title>
        <%= seo?.title || 'Checkout Success - Fourways' %>
    </title>
    <meta name="description" content="<%= seo?.description || 'Order completed successfully at Fourways.' %>">
    <link rel="canonical" href="<%= seo?.canonicalUrl %>">
    <meta name="robots" content="<%= seo?.robots || 'noindex,nofollow' %>">
    <link href="/styles/home.css" rel="stylesheet">
    <link href="/styles/navbar.css" rel="stylesheet">
    <link href="/styles/cart.css" rel="stylesheet">
    <link href="/styles/footer.css" rel="stylesheet">
    <script type="module" src="/scripts/home.js" defer></script>
</head>

<body>
    <%- include('partials/navbar') %>
        <main class="page-wrapper">
            <div class="header__container">
                <h1 class="headline-text">Order Successful!</h1>
                <p>Thanks so much for your order! Your payment confirmation will be sent to your email shortly and
                    you'll be
                    updated when your coffee has shipped. Any questions? Reach out at <a class="success-email__link"
                        href="mailto:orders@fourways.coffee">orders@fourways.coffee</a></p>
            </div>

            <% if (!isWebstoreInMaintenance) { %>
                <%- include('cart') %>
                    <div id="cart-overlay" class="cart-overlay"></div>
                    <% } %>
        </main>
        <%- include('partials/footer') %>
</body>

</html>